extends ../layout

block content
  form(action="/lead/" + lead.id +"/edit", method="POST")
    input(name="lead_email" type="email", placeholder="Your email here for more recipes" value=lead.email)
    button(type="submit") Update Lead