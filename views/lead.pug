doctype html
html(lang="en")

include common/header.pug
  +head("MVP")

  body
    h1 Lead
    p 
      span #{lead.email}
      a(href="/lead/" + lead.id + "/edit") Edit
      |
      form(action="/lead/" + lead.id + "/delete", method="POST")
        button(type="submmit") Delete
    
    include common/footer.pug
    +footer()